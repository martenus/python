while True:
  try:
     x = int(input("vloz prve cislo"))
     y = int(input("vloz druhe cislo"))
     break   
  except ValueError:
     print("zle nedobre")

gcd = 1

for i in range(2,max(x,y)):
  if x%i==0 and y%i==0:
    gcd = i

print(gcd)
